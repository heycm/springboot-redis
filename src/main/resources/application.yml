server:
  port: 8800
spring:
  redis:
    database: 1 #Redis数据库索引[0-15]，默认0
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-active: 8 #最大连接数
        max-wait: -1 #最大等待时间，负值永久
        max-idle: 8 #最大空闲连接
        min-idle: 0 #最小空闲连接
    timeout: 1000 #超时时间
    password: 123456

